# Requirements

You need to install `nvm`, `yarn`, `docker`, `docker-compose`.

Tested on Node v14

## How to run

### Start mongo

```bash
yarn mongo:start
```

### Load data

```bash
yarn load-data
```

### Validate data

```bash
yarn validate
```

### Stop mongo

```bash
yarn mongo:stop
```
